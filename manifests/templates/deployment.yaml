---
# Source: helm for kubernetes/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: srt-88594ebc
  labels:
    app: srt-88594ebc
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: srt-88594ebc
  template:
    metadata:
      labels:
        app: srt-88594ebc
    spec:
      containers:
        - name: srt-88594ebc
          image: docker.io/phengbunath/srt-88594ebc:1
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
